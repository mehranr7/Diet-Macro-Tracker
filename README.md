# Diet-Macro-Tracker
Lightweight Apache Cordova macro tracker (Protein/Carbs/Fat) with Persian (RTL) UI, dark/light theme, and local storage persistence. Foods database is loaded from a published Db.json in the repo, so the app can run fully client-side.
